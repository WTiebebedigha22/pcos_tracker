import 'package:flutter/material.dart';

class SymptomLogPage extends StatelessWidget {
  const SymptomLogPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Symptom Log Page'),
      ),
    );
  }
}